const ORIGIN = 'http://jinsul.co.kr/erp/physical/';

export const SAVE_URL = `${ORIGIN}controller.php`;

// const MANUAL_ADD_FORM_URL = `${ORIGIN}?p=15&mode=add&tp=m&u=${userKey}&w=${getToday(
//   8
// )}`;

// const MANUAL_LIST_URL = `${ORIGIN}?p=15&mode=list&tp=m`;
